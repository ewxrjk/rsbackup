#! /bin/sh
set -e
. /etc/rsbackup/defaults
up=""
for host in $hourly; do
  if ssh -o ConnectTimeout=1 $host true >/dev/null 2>&1; then
    up="$up $host"
  fi
done
if [ "$up" != "" ]; then
  rsbackup --backup $up
fi
